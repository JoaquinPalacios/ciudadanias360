# Copy to `.env.local` for local development (do not commit `.env.local`)

# Public site URL (used for canonical URLs, sitemap, robots, metadataBase)
NEXT_PUBLIC_SITE_URL=https://www.ciudadanias360.com

# Prismic
# If omitted, falls back to `slicemachine.config.json` repositoryName.
NEXT_PUBLIC_PRISMIC_ENVIRONMENT=ciudadanias360

# Enable article routes only once `article_index` + `article` custom types exist in Prismic
NEXT_PUBLIC_PRISMIC_ENABLE_ARTICLES=true

# Contact form (Gmail via Nodemailer)
EMAIL_ADDRESS=example@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# Protect /api/revalidate (Prismic webhook). Use a long random secret.
PRISMIC_REVALIDATE_SECRET=change-me


